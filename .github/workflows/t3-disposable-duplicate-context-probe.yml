name: t3-disposable-duplicate-context-probe

on:
  pull_request:
    types: [synchronize]

permissions:
  contents: read

jobs:
  duplicate:
    name: architecture-policy
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "This disposable duplicate must remain skipped."
