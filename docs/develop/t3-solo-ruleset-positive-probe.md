# Disposable T3 solo-maintainer ruleset probe

This file exists only on a diagnostic pull request. It verifies that an ordinary
documentation change receives the trusted `architecture-policy` check and that
the active `main` ruleset evaluates the exact current pull-request head.

The diagnostic pull request must be closed without merge.
